export interface TaskFormState {
    title: string;
    description: string;
    // tokenGate: string;
    tokenSymbol: string;
    // rewardToken: string;
    // rewardAmount: string;
}

export interface SubmissionType {
    submissionLink: string;
}